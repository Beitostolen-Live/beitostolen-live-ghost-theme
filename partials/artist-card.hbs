{{#if feature_image}}
    <div id="tag-post">
        <a href="{{url}}">
            <img class="post-card-image"
                src="{{img_url feature_image }}"
                alt="{{title}}" />
        </a>
    </div>
{{/if}}